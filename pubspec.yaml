name: medsafe
description: "Intelligent Medication Safety & Adherence Monitoring System"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '^3.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.1
  supabase_flutter: ^2.5.0
  http: ^1.2.0
  flutter_local_notifications: ^17.1.2
  timezone: ^0.9.2
  fl_chart: ^0.65.0
  percent_indicator: ^4.2.3
  intl: ^0.19.0
  uuid: ^4.3.3
  google_fonts: ^6.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
